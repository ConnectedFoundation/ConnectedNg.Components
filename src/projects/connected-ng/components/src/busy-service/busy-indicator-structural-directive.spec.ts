import { BusyIndicatorStructuralDirective } from './busy-indicator-structural-directive';

describe('BusyIndicatorStructuralDirective', () => {
  it('should create an instance', () => {
    expect(new BusyIndicatorStructuralDirective()).toBeTruthy();
  });
});